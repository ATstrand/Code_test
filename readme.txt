created with spring boot.

1.Run application, and it will print in console

formula :
    E = Fixed monthly payment
    b = Interest on a monthly basis     Assuming interest is in percents
    U = Total loan
    p = Number of payments

    E = U[b(1 + b)^p]/[(1 + b)^p - 1]   Assuming it is meant as ^(p-1)

Requirments
Code in a standalone project on GitHub (or similar) that we can access      done
A build tool used to compile, build, test and run project                   done
Readme of how to start the application                                      done
Unit test for testing application logic                                     done
Usage of java.math or similar math dependencies are not allowed             done


tried getting a database included but ran into problems.